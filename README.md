# words_occurrence_counter
C, pthreads, OpenMP. Contador de palavras específicas em um texto.

Simple project comparing a sequential, pthreads and openmp implementation of a word ocurrence counter script in C.
